name: streaming_state
description: A new Flutter package project.
version: 1.0.0-alpha.3
homepage: null

environment:
  sdk: '>=2.12.0 <3.0.0'
  flutter: '>=1.17.0'

dependencies:
  flutter:
    sdk: flutter
  map_stream: ^1.0.0-alpha.4

dev_dependencies:
  flutter_test:
    sdk: flutter

repository: https://github.com/JontiHudson/streaming_state
